{
  "name": "{{.name}}",
  "description": "for SLO entitySelector definition",
  "rules": [
    {
      "enabled": true,
      "valueFormat": "frontendservice",
      "valueNormalization": "Leave text as-is",
      "type": "SELECTOR",
      "entitySelector": "type(SERVICE),toRelationships.calls(type(APPLICATION))"
    }
  ]
}